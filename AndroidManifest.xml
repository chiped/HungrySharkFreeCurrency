<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chinmay.hungrysharkfreecurrency"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Translucent.NoTitleBar" >
        <activity
            android:name="com.chinmay.hungrysharkfreecurrency.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <service
            android:name="com.chinmay.hungrysharkfreecurrency.FloatingHeadService"
            android:label="@string/floating_head_string" >
        </service>
        
        <service
            android:name="com.chinmay.hungrysharkfreecurrency.TransparentSettingsActivity"
            android:label="@string/settings_name" >
        </service>

    </application>

</manifest>
